{
  "description": "A rule targeting the simplified platform \"windows\" applies to a real Graph platform like \"windows10\" — Graph never returns the bare name the baseline schema uses, so this has to be a prefix match, not exact.",
  "entries": [
    {
      "key": "Require BitLocker::windows10",
      "name": "Require BitLocker",
      "cspPath": "./Vendor/MSFT/BitLocker/RequireDeviceEncryption",
      "category": "Encryption",
      "platform": "windows10",
      "values": ["0"],
      "sources": [],
      "conflict": false,
      "state": "Baseline"
    }
  ],
  "rules": [
    {
      "id": "cis-bitlocker",
      "name": "BitLocker required",
      "platform": "windows",
      "path": "./Vendor/MSFT/BitLocker/RequireDeviceEncryption",
      "expect": "1",
      "severity": "critical",
      "rationale": "Unencrypted disks are readable by anyone with physical access.",
      "source": "CIS Windows 10 Benchmark"
    }
  ],
  "expect": [
    {
      "key": "Require BitLocker::windows10",
      "state": "Below baseline"
    }
  ]
}
